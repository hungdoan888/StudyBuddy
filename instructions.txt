Download QT Designer from: https://build-system.fman.io/qt-designer-download
Convert the .ui in the terminal using:

pyuic5 -x test.ui -o test.py
pyuic5 -x studyBuddy.ui -o studyBuddy2.py

To Run Executable, use: python setup.py build

pip install pyqt5
pip install pyqt5-tools

Example GUI:
from PyQt5 import QtWidgets
from PyQt5.QtWidgets import QApplication, QMainWindow
import sys

class MyWindow(QMainWindow):
    
    def __init__(self):
        super(MyWindow, self).__init__()
        self.setGeometry(200, 200, 800, 600)
        self.setWindowTitle("My First Python Application")
        self.initUI()
        
    def initUI(self):
        self.label = QtWidgets.QLabel(self)
        self.label.setText("My First Label")
        self.label.move(50, 50)
        
        self.b1 = QtWidgets.QPushButton(self)
        self.b1.setText("click me")
        self.b1.clicked.connect(self.clickedb1)    
               
    def clickedb1(self):
        self.label.setText("You pressed the button")
        self.update()
        
    def update(self):
        self.label.adjustSize()
        
    
def window():
    app = QApplication(sys.argv)
    win = MyWindow()
    win.show()
    sys.exit(app.exec_())
    
window()

https://www.youtube.com/watch?v=HYV81L7qd6M&ab_channel=TechWithTim
    